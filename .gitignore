node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
.netlify/

# personal, not the project
.claude/settings.local.json

# marketing drafts and social assets, not for the public repo
assets/social/
# the poster generator: a local tool, and it needs sharp, which is not a dependency
scripts/poster.tsx
